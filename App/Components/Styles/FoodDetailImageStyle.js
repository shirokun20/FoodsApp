import { connect } from '@Themes/OsmiProvider'

export default connect({
  container: 'self-center rounded-lg bg-white shadow',
  errorContainer: 'w/50 h-190 rounded-lg bg-gray-800 justify-center',
  errorText: 'text-white text-center font-bold',
  image: 'w/50 h-190 rounded-lg',
})
